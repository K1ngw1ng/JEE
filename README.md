# JEE
A Python Utility for underacting improperly redacted or non-flattened PDFs to txt

# Instructions

- Install Dependencies  
`pip install pymupdf tqdm`
- Run Script  
`python unredact.py ./files -t 8`

